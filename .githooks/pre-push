#!/usr/bin/env bash
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
python3 -m unittest discover -s tests -v
./scripts/vendor-automatis-commands --check
exec ./scripts/build-automatis-release --check
